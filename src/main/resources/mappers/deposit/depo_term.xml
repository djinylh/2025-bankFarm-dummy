<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.bankfarm_dummy.bankfarm_dummy.depo.DepoTermMapper">

    <insert id="insertTermContract">
        INSERT INTO depo_contract_term
        SET
        depo_contract_id = #{contractId},
        depo_term_tp = #{termTp},
        depo_term_dt = #{termDt}
    </insert>

    <select id="selectTermContract">
        SELECT depo_contract_id, depo_maturity_dt
        FROM depo_contract
        WHERE depo_active_cd = 'CS002'
    </select>

</mapper>